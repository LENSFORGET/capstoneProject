# -----------------------------------------------------------------------
# 小红书爬取组件 - 本地运行 Python 依赖
# 用法：pip install -r requirements-xhs-local.txt
#
# 不在本地虚拟环境安装 NAT editable 包；
# NAT 由项目当前 Anaconda 环境提供，避免双 NAT 版本互相干扰。
# -----------------------------------------------------------------------

mcp[cli]>=1.0
psycopg2-binary>=2.9
aiorwlock~=1.5
langchain-core
requests
